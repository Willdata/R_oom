#If we know the function name, but have no idea how to use it, for example
?mean

#If we want to ask operator, we need to add '' to squeeze it
?'+'
?'*'
?'=='

#If we do not know the exact name of the function we desire to use, for example
apropos("mea")
